# Copy this file to .env and set your token.
# Only SYNC_TOKEN is required. The app handles auth via its own login screen.

# Sync API token — shared secret between the frontend and sync server.
# Generate with: openssl rand -hex 32
SYNC_TOKEN=replace-with-your-secret-token
